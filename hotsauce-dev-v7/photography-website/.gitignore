node_modules
.next
.next-dev
.env*.local
.DS_Store
*.log

# Development logs
logs/

# Lock files (if switching package managers)
package-lock.json
yarn.lock

# Cache directories
node_modules/.cache
.next/cache
# Env template is safe to commit — it holds no secrets
!.env.example

# Regenerated by next on every dev/build; its distDir reference flips
# between .next and .next-dev depending on which ran last
next-env.d.ts
